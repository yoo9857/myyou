# SFX_PREROLL handoff

Import `sfx_preroll_stem.m4a` on one separate CapCut audio track at 00:00:00.000. Keep it at 0 dB because the approved low-level assets and project mix gain are already applied.

Use `../sfx_timeline.csv` only when individual WAV placement is needed. Do not import both the stem and individual effects.
